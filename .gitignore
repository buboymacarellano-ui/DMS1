node_modules/
logs/
*.sqlite
*.sqlite-wal
*.sqlite-shm
data/shop.backup.*.sqlite
data/data.backup.*.json
data/sqlite-path.txt
.env
.env.*
npm-debug.log
.DS_Store

# Runtime data dir (SQLite + JSON snapshot) when LOCALAPPDATA is unset
AE-DMS/
data/AE-DMS/
data/data-snapshot.json
session-secret.txt

# UI reskin baseline screenshots (local reference only)
docs/superpowers/plans/baselines/

# Seeded design-canvas payload — 2.5MB build artifact, re-seed from the
# .dc.html working files with the design skill's seed-canvas helper.
docs/superpowers/plans/canvas-1/ae-dms-foundations.html
